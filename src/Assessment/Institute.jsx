import React from "react";

const Institute = ({ name, location, establishedYear }) => {
  return (
    <>
      <p>name{name}</p>
      <p>location{location}</p>
      <p>establishedYear{establishedYear}</p>
    </>
  );
};

export default Institute;
